{
  "//": [
    "Regras de seguranca do Realtime Database do Simplificando as Entregas.",
    "",
    "Como publicar: console do Firebase > Realtime Database > aba Regras >",
    "cola este conteudo > Publicar. (Ou 'firebase deploy --only database' com a CLI.)",
    "",
    "ANTES de publicar, o login anonimo precisa estar ligado no console:",
    "Authentication > Sign-in method > Anonymous > Ativar. Sem isso o app",
    "inteiro para de sincronizar, porque toda regra aqui exige auth != null.",
    "",
    "O que estas regras garantem:",
    "  1. Ninguem sem credencial le ou escreve nada (antes, em modo teste, o",
    "     banco era publico: nome de quem recebeu, endereco e codigo de",
    "     rastreio de todas as rotas, e qualquer um podia apagar tudo).",
    "  2. Nao da pra listar as rotas existentes. 'lotes' e 'rotas_registradas'",
    "     nao tem .read no no pai, so nos filhos - entao so le quem ja sabe o",
    "     codigo da rota. Isso e o que impede alguem de descobrir uma rota e",
    "     depois ler os dados dela.",
    "  3. Toda foto tem que ser uma URL do Cloudinary. Isso fecha, do lado do",
    "     banco, o caminho de injetar HTML pela URL da foto.",
    "  4. Cor tem que ser hexadecimal de verdade, pelo mesmo motivo: ela vai",
    "     direto pra um style= na tela.",
    "  5. Os dois contadores so podem subir de 1 em 1, entao ninguem zera nem",
    "     inventa um numero.",
    "",
    "Limite conhecido e proposital: o codigo da rota continua sendo o segredo",
    "compartilhado entre os colegas - quem tem o codigo tem acesso completo",
    "aquela rota, inclusive pra apagar. Isso espelha como voces trabalham hoje",
    "(o codigo passa de boca em boca). Para ir alem disso seria preciso um dono",
    "por rota e convite de colega, que e um redesenho bem maior.",
    "",
    "Use codigos de rota longos e nao obvios - eles sao a senha da rota."
  ],

  "rules": {
    ".read": false,
    ".write": false,

    "contador_global": {
      ".read": "auth != null",
      ".write": "auth != null",
      ".validate": "newData.isNumber() && newData.val() >= 0 && (!data.exists() || (newData.val() > data.val() && newData.val() <= data.val() + 1))"
    },

    "rotas_total": {
      ".read": "auth != null",
      ".write": "auth != null",
      ".validate": "newData.isNumber() && newData.val() >= 0 && (!data.exists() || (newData.val() > data.val() && newData.val() <= data.val() + 1))"
    },

    "rotas_registradas": {
      "$rota": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.isNumber()"
      }
    },

    "lotes": {
      "$rota": {
        ".read": "auth != null",
        ".write": "auth != null",

        "faixas": {
          "ts": { ".validate": "newData.isNumber()" },
          "ruas": {
            "$idx": {
              "id": { ".validate": "newData.isString() && newData.val().length <= 64" },
              "nome": { ".validate": "newData.isString() && newData.val().length <= 120" },
              "fragmentar": { ".validate": "newData.isBoolean()" },
              "maxPar": { ".validate": "newData.isNumber()" },
              "maxImpar": { ".validate": "newData.isNumber()" },
              "corteManualPar": { ".validate": "newData.isNumber()" },
              "corteManualImpar": { ".validate": "newData.isNumber()" },
              "cores": {
                "$campo": {
                  ".validate": "newData.isString() && newData.val().matches(/^#[0-9a-fA-F]{3}([0-9a-fA-F]{3})?$/)"
                }
              },
              "cepVistos": {
                "$cep": { ".validate": "newData.isNumber()" }
              },
              "$outro": { ".validate": false }
            }
          },
          "$outro": { ".validate": false }
        },

        "lista": {
          "$data": {
            ".validate": "$data.matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)",
            "texto": { ".validate": "newData.isString() && newData.val().length <= 500000" },
            "ts": { ".validate": "newData.isNumber()" },
            "$outro": { ".validate": false }
          }
        },

        "pacotes": {
          "$codigo": {
            "separado": { ".validate": "newData.isBoolean()" },
            "entregue": { ".validate": "newData.isBoolean()" },
            "devolvido": { ".validate": "newData.isBoolean()" },
            "ts": { ".validate": "newData.isNumber()" },
            "dia": { ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)" },
            "$outro": { ".validate": false }
          }
        },

        "entregas": {
          "$data": {
            ".validate": "$data.matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/)",
            "$codigo": {
              "rua": { ".validate": "newData.isString() && newData.val().length <= 200" },
              "numero": { ".validate": "(newData.isString() && newData.val().length <= 20) || newData.isNumber()" },
              "nome": { ".validate": "newData.isString() && newData.val().length <= 200" },
              "chave": { ".validate": "newData.isString() && newData.val().length <= 200" },
              "quemRecebeu": { ".validate": "newData.isString() && newData.val().length <= 120" },
              "ts": { ".validate": "newData.isNumber()" },
              "fotos": {
                "$slot": {
                  ".validate": "newData.isString() && newData.val().beginsWith('https://res.cloudinary.com/')"
                }
              },
              "fotoPacoteUrl": {
                ".validate": "newData.isString() && newData.val().beginsWith('https://res.cloudinary.com/')"
              },
              "$outro": { ".validate": false }
            }
          }
        },

        "fachadas_permanentes": {
          "$chave": {
            "rua": { ".validate": "newData.isString() && newData.val().length <= 200" },
            "numero": { ".validate": "(newData.isString() && newData.val().length <= 20) || newData.isNumber()" },
            "fotoFachadaUrl": {
              ".validate": "newData.isString() && newData.val().beginsWith('https://res.cloudinary.com/')"
            },
            "ts": { ".validate": "newData.isNumber()" },
            "$outro": { ".validate": false }
          }
        },

        "$outro": { ".validate": false }
      }
    }
  }
}
